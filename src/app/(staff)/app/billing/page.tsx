import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Time & billing" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Time & billing"
      description="Time entries, WIP totals, and simple invoice generation."
      milestone="M7"
    />
  );
}
