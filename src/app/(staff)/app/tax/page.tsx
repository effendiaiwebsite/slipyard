import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Returns" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Returns"
      description="Engagement list across years with bulk status operations."
      milestone="M3"
    />
  );
}
