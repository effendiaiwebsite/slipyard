import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "CRA authorizations" };

export default function Page() {
  return (
    <PlaceholderPage
      title="CRA authorizations"
      description="Coverage dashboard, needs-attention list, certification-page packs."
      milestone="M7"
    />
  );
}
