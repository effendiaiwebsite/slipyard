import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Document intake" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Document intake"
      description="Vault front door: drag-drop, intake queue, checklists, missing-docs dashboard."
      milestone="M3"
    />
  );
}
