import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Clients" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Clients"
      description="Client grid, households, tags, notes, contact log, engagements."
      milestone="M2"
    />
  );
}
