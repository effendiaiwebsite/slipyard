import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "E-signatures" };

export default function Page() {
  return (
    <PlaceholderPage
      title="E-signatures"
      description="Field placement, remote and in-person signing, executed PDFs with audit page."
      milestone="M6"
    />
  );
}
