import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Settings" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Settings"
      description="Org profile, employees, billing, templates, AI toggle, retention, data import."
      milestone="M1"
    />
  );
}
