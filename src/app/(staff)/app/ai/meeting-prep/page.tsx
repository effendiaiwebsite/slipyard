import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Meeting prep" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Meeting prep"
      description="One-page printable brief per client before a meeting."
      milestone="M8"
    />
  );
}
