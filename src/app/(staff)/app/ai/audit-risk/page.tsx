import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Audit risk" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Audit risk"
      description="Staff-facing advisory flags: deterministic rules plus AI narrative."
      milestone="M8"
    />
  );
}
