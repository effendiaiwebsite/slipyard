import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Workflow board" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Workflow board"
      description="Kanban of engagements by status for the active tax year."
      milestone="M2"
    />
  );
}
