import { PlaceholderPage } from "@/components/staff/placeholder-page";

export const metadata = { title: "Messaging" };

export default function Page() {
  return (
    <PlaceholderPage
      title="Messaging"
      description="Templates, reminder policies, mass send, STOP handling, outbox."
      milestone="M5"
    />
  );
}
